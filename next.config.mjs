/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  images: {
    domains: ['cdn11.bigcommerce.com', 'images.unsplash.com'],
  },
};

export default nextConfig;
